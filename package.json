{
  "name": "slo-scroller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite --open",
    "test": "node --test",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist --repo https://x-access-token:${GITHUB_TOKEN}@github.com/$GITHUB_REPOSITORY.git"
  },
  "devDependencies": {
    "vite": "^4.5.14",
    "gh-pages": "^6.3.0"
  }
}
