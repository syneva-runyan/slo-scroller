// Duration of each outage incident in seconds when recorded
export const OUTAGE_SECONDS = 1.6;

// Default rolling window size (in seconds) for calculating availability metrics
export const DEFAULT_WINDOW_SECONDS = 10;

// Small tolerance value for floating-point precision when comparing availability to target
export const TARGET_EPSILON = 0.0001;
